---
- environment:
    SC_HOST: "{{ sc_host }}"
    SC_USERNAME: "{{ sc_username }}"
    SC_PASSWORD: "{{ sc_password }}"
    SMB_SERVER: "{{ smb_server }}"
    SMB_SHARE: "{{ smb_share }}"
    SMB_USERNAME: "{{ smb_username }}"
    SMB_PASSWORD: "{{ smb_password }}"

  block:
# ----------------------------------Cleanup------------------------------------------------------------------------
    - name: Run cleanup test
      local_action: command targets/aa_aa/tasks/smb_cleanup.sh
